{
    "javascriptLines":[
        "function Main(){ ",
        "import React from \"react\"; ",
        "const mongoose = require('mongoose'); ",
        "const app = express(); ",
        "const funcName = () =>{ ",
        "console.log('Hello World'); ",
        "var n = null; ",
        "var canv = document.getElementById(\"root\"); ",
        "alert(\"Hello There\"); ",
        "let search = document.querySelector(\"#s\"); ",
        "axios.get(url, params) ",
        "ul.classList.add(\"list\"); ",
        "console.log(`${youName} is amazing`); ",
        "if (!start || !target || start === target) { ",
        "nodes[start].distance = 0; ",
        "let unvisitedNodes = Object.keys(nodes); ",
        "while (unvisitedNodes.length) { ",
        "let currentNode = closestNode(nodes, unvisitedNodes); ",
        "currentNode = closestNode(nodes, unvisitedNodes) ",
        "nodesToAnimate.push(currentNode); ",
        "return \"success!\"; ",
        "index = i; ",
        "if (!currentClosest || current.total > nodes[unvisited[i]].total) { ",
        "unvisitedNodes.splice(index, 1); ",
        "let coordinates = id.split(\"-\"); ",
        "if (boardArray[x + 1] && boardArray[x + 1][y]) { ",
        "function getDistance(nodeOne, nodeTwo) { ",
        "let y2 = parseInt(targetCoordinates[1]); ",
        "if (x2 < x1 && y1 === y2) { ",
        "return [1.5, null, \"down\"]; ",
        "let xChange = Math.abs(xOne - xTwo); ",
        "module.exports = astar; ",
        "nodes[start].direction = \"up\"; ",
        "let relevantIds = [board.start, board.target]; ",
        "Object.keys(board.nodes).forEach(node => { ",
        "board.wallsToAnimate.push(currentHTMLNode); ",
        "let possibleRows = []; ",
        "let randomRowIndex = Math.floor(Math.random() * possibleRows.length); ",
        "if (rowEnd - (currentRow + 2) > colEnd - colStart) { ",
        "let r = parseInt(node.split(\"-\")[0]); ",
        "module.exports = recursiveDivisionMaze; ",
        "const weightedSearchAlgorithm = require(\"weightedAlgorithm.js\"); ",
        "let speed = board.speed === \"fast\" ? 0 : board.speed === \"average\"; ",
        "if (type === \"weighted\") { ",
        "console.log(\"Failure.\"); ",
        "board.reset() ",
        "board.toggleButtons(); ",
        "timeout(index + 1); ",
        "let currentHTMLNode = document.getElementById(currentNode.id); ",
        "function Board(height, width) { ",
        "this.previouslyPressedNodeStatus = null; ",
        "Board.prototype.initialise = function() { ",
        "let board = document.getElementById(\"board\"); ",
        "currentElement.onmousedown = (e) => { ",
        "e.preventDefault(); ",
        "let r = parseInt(coordinates[0]); ",
        "app.get(\"/\", (req, res) => { ",
        "toDispatch.push([start, left, right]); ",
        "setTimeout(() => { ",
        "function mergeSort(stateArray, dispatch, speed) { ",
        "actualFirst = mergeSort(first, toDispatch, start, indexHalf - 1, obj) ",
        "econd[0][1] = indexToPush++; ",
        "sortedArray.push(second.shift()); ",
        "dispatch(setCurrentSorted(array.map((num, index) => index))); ",
        "while (!sorted) { ",
        "if (array[i] > array[i + 1]) { "
        
    ]
}